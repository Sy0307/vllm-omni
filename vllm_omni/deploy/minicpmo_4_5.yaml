# Compatibility entry point for MiniCPM-o 4.5.
#
# The implementation now requires the split Talker and Code2Wav stages; keep
# the tuned topology in one canonical file so the default and explicit deploy
# cannot drift. See the recipe's migration section for the topology change.
base_config: minicpmo_4_5_batching.yaml
